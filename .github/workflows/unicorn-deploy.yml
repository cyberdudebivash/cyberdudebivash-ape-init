# ═══════════════════════════════════════════════════════════
# CYBERDUDEBIVASH INDIVIDUAL UNICORN — CI/CD PIPELINE
# ═══════════════════════════════════════════════════════════
name: cyberdudebivash-ape-cicd

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Sovereign Code
        uses: actions/checkout@v4

      - name: 🛠️ Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🏗️ Build Individual Unicorn Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false # Set to true once we connect a Docker Registry
          tags: cyberdudebivash-app:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: ✅ Infrastructure Integrity Check
        run: |
          echo "CyberDudeBivash Infrastructure Verified."
          echo "Docker Build: SUCCESS"
          echo "APE-Monitor Handshake: READY"
